# Incidents

Incident dataset phục vụ đánh giá RCA.

Mỗi incident nên có:

- `inject script`
- `expected symptom`
- `ground truth`
- `expected evidence`
- `cleanup script`
- `sample RCA output`
- kết quả chạy theo từng baseline
