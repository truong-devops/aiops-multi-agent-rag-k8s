# Tests

Thư mục test liên service và test vận hành.

- `e2e`: test luồng login, upload, processing, feed/admin.
- `smoke`: test sau deploy Kubernetes.
- `load`: test latency, queue lag, media processing pressure.

Unit test nằm trong từng service/app tương ứng.
